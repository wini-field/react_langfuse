// src/pages/Tracing/types.js
import { ReactNode } from 'react';

// JSON-serializable type for metadata
export const JsonValue = undefined;

// Type for a single observation from the detail API
export const Observation = undefined;

// Type for a single score from the detail API
export const Score = undefined;

// Standalone type for the full trace details
export const TraceWithFullDetails = undefined;

// Type for an item in the trace list
export const Trace = undefined;

// Other existing types
export const SessionData = undefined;

export const TraceData = undefined;

export const Column = undefined;